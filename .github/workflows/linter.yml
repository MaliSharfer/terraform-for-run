name:  💄 terraform Lint
on: 
  push:
    branches: [feature/linter-in-terraform]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout
        uses: actions/checkout@v4
     
      - name: Lint Terraform
        uses: actionshub/terraform-lint@v4 

      # - name: 💄 Run Ruff
      #   run: |
      #     ruff check --output-format=github .
      #     ruff format --check .


# name: terraform-lint

# on: [push, pull_request]

# jobs:
#   delivery:

#     runs-on: ubuntu-latest

#     steps:
#     - name: Check out code
#       uses: actions/checkout@main
#     - name: Lint Terraform
#       uses: actionshub/terraform-lint@main          